apiVersion: v1
kind: Service
metadata:
  name: ecommerce-frontend
  namespace: ecommerce

spec:
  selector:
    app: ecommerce-frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: LoadBalancer